{
  "name": "jfront-components",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "cpx": "^1.5.0",
    "rimraf": "^3.0.2",
    "@types/styled-components": "^5.0.1",
    "@types/react-datepicker": "^2.11.0"
  },
  "peerDependencies": {
    "react": "16.x",
    "react-dom": "16.x",
    "styled-components": "^5.0.1",
    "date-fns": "^2.14.0",
    "@types/react-datepicker": "^2.11.0"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc & npm run copy-resources",
    "copy-resources": "cpx \"src/**/*.!(ts|tsx)\" dist",
    "clean-build": "npm run clean && npm run build"
  },
  "dependencies": {
    "date-fns": "^2.14.0",
    "react-datepicker": "^2.16.0",
    "typescript": "^3.9.3"
  }
}
